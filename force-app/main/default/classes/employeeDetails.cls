public class employeeDetails {
    public void showDetails(string name, string email, string phone) {

        system.debug('The name of the employee is : ' + name);
        system.debug('The email of the employee is : ' + email);
        system.debug('The phone number of the employee is : ' + phone);
    }
}