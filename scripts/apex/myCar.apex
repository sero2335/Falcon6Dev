String brand = 'Volkswagen';
system.debug(brand);
String model = 'Bora';
system.debug(model);
String color = 'Gray';
system.debug(color);
String myCar = 'My Car is ' + brand + ' ' + model + ' ' + color;
system.debug(myCar);